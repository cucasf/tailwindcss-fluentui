{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "postcss.config.js",
  "scripts": {
    "test": "npx tsc -p ../ && tailwind -i tailwind.css -o out/styles.css --config tailwind.config.js --verbose --env development",
    "test-watch": "npx tsc -p ../ && tailwind -i tailwind.css -o out/styles.css --config tailwind.config.js --watch --verbose"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "@tailwindcss/fluentui": "file:..",
    "autoprefixer": "^10.4.15",
    "cssnano": "^6.0.1",
    "postcss": "^8.4.29",
    "postcss-cli": "^10.1.0",
    "postcss-import": "^15.1.0",
    "postcss-nesting": "^12.0.1",
    "tailwindcss": "^3.3.3"
  }
  
  
}
